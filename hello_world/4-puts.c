#include <stdio.h>
/**
 * main - function that prints the correct output
 * Prints "Programming is like building a multilingual puzzle"
 * followed by a new line.
 *
 * Return: 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
