#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @x: integer being computed
 *
 * Return: absolute value of integer
 */

int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-x);
}
